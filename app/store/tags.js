export const TAG_TYPES = {
  AUTH: 'Auth',
  TENANT: 'Tenant',
  USER: 'User',
  ASSET: 'Asset',
};

// Exporting as an array for the tagTypes array in createApi
export const tagTypesList = Object.values(TAG_TYPES);
